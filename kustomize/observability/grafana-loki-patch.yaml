---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: loki
spec:
  values:
    loki:
      enabled: true
    promtail:
      enabled: true
    fluent-bit:
      enabled: false
    grafana:
      enabled: false
    prometheus:
      enabled: false
